# demo
first repo for learning
<br>
first we need to add chnage then commit it to update it permanently.
<br>
use git add <filname> to add particular file or use git add . to add all files.
<br>
git commit -m "message anything"
<br>
git push main origin to push the repos, if error occurs the use git pull origin main and then repeat the process